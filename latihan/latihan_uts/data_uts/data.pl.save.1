#!/usr/bin/perl

@nama=();
@nrp=();
@alamat=();
@nomor=();
@string=();
$number=0;
$n=<stdin>;


while ($number < $n ) {

$nrp[$number]=<>;
$nama[$number]=<>;
$alamat[$number]=<>;
$nomor[$number]=<>;

$number += 1;

}

$number = 0;

while ($number < $n ) {
chomp;
$string[$number] = join "|" ,($nrp[$number$nama[$number],$alamat[$number],$nomor[$number] );
print "$string[$number]";
$number += 1;
}




