#!/usr/bin/python
import csv

reader = csv.DictReader(open('API_SI.POV.GINI_DS2_en_csv_v2.csv'))
#with open('API_SI.POV.GINI_DS2_en_csv_v2.csv') as f:
 #   f.readline() # ignore first line (header)
  #  mydict = dict(csv.reader(f, delimiter=','))

#print reader
#print type(reader)
for row in reader:
	print row['Country Code'],

